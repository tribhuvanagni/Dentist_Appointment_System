## Frontend env (Vercel/Netlify)
# In dev you can leave this empty because `proxy` in `frontend/package.json` points to localhost:5000.
# In production you MUST set it to your deployed backend base URL.
REACT_APP_API_URL=https://your-backend.onrender.com

REACT_APP_API_URL=

